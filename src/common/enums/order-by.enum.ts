// ============================================
// src/common/enums/order-by.enum.ts
// ============================================
export enum OrderDirection {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum UserOrderBy {
  CREATED_AT = 'createdAt',
  UPDATED_AT = 'updatedAt',
  EMAIL = 'email',
  FIRST_NAME = 'firstName',
  LAST_NAME = 'lastName',
}